# Plan-03 — Task Checklist

Granular tasks per phase. Each is < 1 h. Tick as completed.

> Companion to `2026-05-22-plan-03-frontend-mvp.md`. Use `superpowers:subagent-driven-development` or `superpowers:executing-plans` to drive.

---

## Open Decisions (resolved 2026-05-22)

- [x] Icon set: **lucide** subset, inline SVG
- [x] History v1: **session-only** (no backend change)
- [x] Delete-entity: **add `DELETE /api/entities/{entity_id}` in P3** (entities.py currently GET-only)

---

## P1 — Skeleton + Theme

- [ ] Create branch `feature/plan-03-frontend-mvp`
- [ ] `mkdir addon/web && cd addon/web && pnpm init`
- [ ] Install deps: `pnpm add lit @lit/context`, `pnpm add -D vite typescript @types/node eslint eslint-plugin-lit`
- [ ] `tsconfig.json` strict + ES2022 + `noUncheckedIndexedAccess`
- [ ] `vite.config.ts` with `base: './'`, dev-proxy `/api` + `/ws` → `localhost:8099`
- [ ] `.eslintrc.cjs` with lit rules
- [ ] `package.json` scripts: `dev`, `build`, `lint`, `typecheck`
- [ ] `index.html` shell — read theme from localStorage before paint (inline script ≤10 LOC)
- [ ] `src/main.ts` mount `<knx-app>`
- [ ] `src/styles/tokens.css` — paste from plan doc
- [ ] `src/styles/reset.css` + Google Fonts Roboto/Roboto Mono
- [ ] `src/theme.ts` — `get/set/subscribe`, drives `data-theme`
- [ ] `src/router.ts` — hash router for 3 routes
- [ ] `src/components/knx-app.ts` — layout shell
- [ ] `src/components/knx-topbar.ts` — brand + tabs + theme-toggle + conn-status (mock for now)
- [ ] `src/components/knx-tabs.ts` — generic tab-bar
- [ ] `src/components/ui/btn.ts`, `pill.ts`, `card.ts`, `live-dot.ts`, `icon.ts`
- [ ] Wire 3 empty placeholder views
- [ ] `pnpm dev` shows working shell with theme toggle
- [ ] **Backend change:** `addon/app/main.py` mounts `StaticFiles` + catch-all SPA fallback (after all routers)
- [ ] `Dockerfile`: add `frontend-builder` stage with node:20-alpine + pnpm, copy `dist/` to `/app/web/`
- [ ] `.dockerignore`: add `web/node_modules`, `web/dist`
- [ ] Local build: `docker build -t knx-test addon/` succeeds
- [ ] Bump `addon/config.yaml` to `version: "0.1.0-dev"`
- [ ] Install local add-on on Skynet HA, open Ingress → see shell with theme toggle working
- [ ] Commit P1, push branch

## P2 — Convert Flow

- [ ] `src/api/types.ts` — types matching Plan-02 Pydantic (YamlFile, ParseResult, DryRunEntry, CommitResultEntry, EntitySummary)
- [ ] `src/api/client.ts` — wrapper with `credentials: 'include'`, error envelope, 401 hard-reload
- [ ] `src/state/store.ts` — Lit Context, `AppState` shape from plan
- [ ] `src/views/convert-view.ts` — layout: tiles + stepper + step-host
- [ ] `src/views/convert/domain-tiles.ts` — 4 tiles, counts hardcoded then wired to `api.entities.list()`
- [ ] `src/views/convert/stepper.ts` — 4-step visual with `--progress` CSS var
- [ ] `src/views/convert/step-file.ts` — `api.yaml.list()`, click → setSelectedFile + advance
- [ ] `src/views/convert/step-parse.ts` — `api.yaml.parse(path)`, render meta
- [ ] `src/views/convert/step-dryrun.ts` — `api.convert.dryRun({path, domain})`, render table from mockup
- [ ] `src/views/convert/step-commit.ts` — `api.convert.commit(...)`, success state + new entity_ids
- [ ] Disable next-step button until current step succeeds
- [ ] Error toasts on 4xx/5xx with backend `detail`
- [ ] Manually test entire flow against Skynet HA with `light_test.yaml`
- [ ] Commit P2

## P3 — Entities + WS Live

- [ ] Verify backend has `DELETE /api/entities/{entity_id}` — add if missing (separate small commit)
- [ ] `src/api/ws.ts` — `WsClient` class with reconnect backoff
- [ ] `src/views/entities-view.ts` — `api.entities.list()` table from mockup
- [ ] WsClient connects on view-mount, disconnects on unmount
- [ ] State column updates live (find row by entity_id, set state + last_changed)
- [ ] Topbar live-dot reactive to `ws.status`
- [ ] "Delete selected" — confirm dialog (browser `confirm()` for v1) → call delete per id → refetch list
- [ ] Manual test: toggle light from HA UI, see state change in Entities tab without refresh
- [ ] Commit P3

## P4 — History

- [ ] `src/views/history-view.ts` — render `recentCommits` from store
- [ ] After each `convert.commit`, push response into `recentCommits`
- [ ] Render table from mockup (#, time, kind, status, entries)
- [ ] Row-click expands JSON-preview inline (no modal)
- [ ] Filter dropdowns rendered, non-functional (P4-stretch decision)
- [ ] **Stretch (if Open Decision chose "add endpoint"):** backend `GET /api/migrations` + wire it
- [ ] Commit P4

## P5 — Polish + Smoke

- [ ] `pnpm build` — measure gzipped size, must be < 200 KB total JS
- [ ] If over: dynamic import for stepper steps
- [ ] Lighthouse-style sanity in browser DevTools: no console errors, no failed requests, contrast OK both themes
- [ ] Test theme persistence: reload preserves choice
- [ ] Test deep-link: bookmark `#entities`, reload, lands on Entities tab
- [ ] Test WS reconnect: kill backend, see status flip, restart backend, see status recover
- [ ] Write `docs/active/webapp/2026-05-22-plan-03-smoke-procedure.md` (mirror plan-02-smoke format)
- [ ] CI: add `frontend` job to `.github/workflows/ci.yml`, fail if build/lint/typecheck fails
- [ ] Add `frontend` to required checks via `gh api -X PATCH .../branches/main/protection`
- [ ] Bump `addon/config.yaml` to `version: "0.1.0"` (drop `-dev`)
- [ ] Update `addon/app/main.py` FastAPI `version="0.1.0"`
- [ ] Execute smoke procedure on Skynet HA, save result to `2026-05-22-plan-03-smoke.md`
- [ ] Open PR, wait green CI
- [ ] Squash-merge to `main`, tag `v0.1.0-frontend-mvp`
- [ ] Delete feature branch local + remote
- [ ] Clean up smoke fixtures (entity, yaml file, LLT if any)

---

## Out-of-Scope Reminders

Do **not** add during Plan-03:
- Multi-domain Convert (only `light`)
- YAML editor
- Login screen
- i18n
- Snapshot tests
- Playwright E2E
- Service worker / offline

These belong to Plan-04+.
